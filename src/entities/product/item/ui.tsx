
interface ProductItemProps {
    category: string;
    title: string;
    description: string;
    price: number;
}

export const ProductItem = ({ category, title, description, price }: ProductItemProps) => {
    return <div></div>
}